Extract the following bibliographic information from the paper. Format each line exactly as shown, with the field name followed by a colon and the value. Do not add any additional text or formatting.

paper_title: [exact title]
paper_authors: [all authors, comma separated]
paper_publication_date: [publication date in YYYY-MM format if available, else YYYY]
paper_doi: [DOI if present, else "none"]
paper_venue: [journal/conference name if present, else "preprint"]

Paper content:
{{CONTENT}}
